# Git commands for lab1

```bash
git clone https://github.com/<your-user>/NUPP_NET_2025_<group>_TK_<surname>_Lab.git
cd NUPP_NET_2025_<group>_TK_<surname>_Lab
git checkout -b lab1

# copy the solution files into this repository

git add .
git commit -m "Add laboratory work 1"
git push origin lab1
```

After that create a Pull Request from `lab1` into `master` on GitHub.
